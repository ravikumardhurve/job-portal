"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { BriefcaseBusiness, ChevronRight, MapPin } from "lucide-react";

type Application = { id: string; status: string; appliedAt: string; job?: { id: string; title: string; company: string; city: string } };
export function AppliedJobs() { const [applications, setApplications] = useState<Application[]>(); useEffect(() => { fetch("/api/candidate/applications").then(async (response) => { if (response.ok) { const result = await response.json() as { data: Application[] }; setApplications(result.data); } }); }, []); if (!applications) return <p className="text-sm text-[#5C6B63]">Loading your applications...</p>; if (!applications.length) return <div className="mt-5 rounded-lg border border-dashed border-[#DCE8E1] p-6 text-center"><BriefcaseBusiness size={24} className="mx-auto text-[#157A4A]" /><p className="mt-3 font-bold">No applications yet</p><p className="mt-2 text-sm text-[#5C6B63]">Search a role and apply once your profile is ready.</p></div>; return <div className="mt-5 grid gap-3">{applications.map((application) => <article key={application.id} className="flex flex-wrap items-center justify-between gap-4 rounded-lg border border-[#DCE8E1] p-4"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-lg bg-[#EEF5F0] text-[#157A4A]"><BriefcaseBusiness size={18} /></span><div><h3 className="font-bold">{application.job?.title ?? "Job application"}</h3><p className="mt-1 flex items-center gap-1 text-xs text-[#5C6B63]"><MapPin size={13} />{application.job ? `${application.job.company} | ${application.job.city}` : "Job details unavailable"}</p></div></div><div className="text-right"><span className="rounded-full bg-[#E7EFFC] px-2.5 py-1 text-[11px] font-bold text-[#2F5AAE]">{application.status.replaceAll("_", " ")}</span><Link href={application.job ? `/jobs/${application.job.id}` : "/jobs"} className="mt-2 flex items-center justify-end gap-1 text-xs font-bold text-[#0F5C38]">View job <ChevronRight size={13} /></Link></div></article>)}</div>; }